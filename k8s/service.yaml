apiVersion: v1
kind: Service
metadata:
  name: ord-reference-app
  labels:
    app: ord-reference-app
spec:
  type: NodePort
  selector:
    app: ord-reference-app
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
